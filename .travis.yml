sudo: required
language: ruby
rvm:
 - 2.4.2

language: python
python:
  - "2.7"
  - "3.6"
install:
  - pip install -r requirements.txt

# This is not a python project. So, no tests to run. But it fails if I don't have a script key
script:
  - echo "No need to run python tests"

after_success:
- bundle exec codeclimate-test-reporter
